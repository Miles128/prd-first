{# 通用渲染模板:按 section 分组。
   trim_blocks/lstrip_blocks 已在 render.py 开启,标签后的换行会被自动吃掉。
#}
# {{ template.name }} — 产品需求文档

> 项目类型:**{{ template.type }}**
> 创建时间:{{ meta.created_at }}
> 更新时间:{{ meta.updated_at }}

---
{% for section_name, fields in sections.items() %}

## {{ section_name }}
{% for field in fields %}

### {{ field.label }}{% if field.required %} `必填`{% endif %}

{{ get_render(field.key) }}
{% endfor %}
{% endfor %}

---

_本 PRD 由 `prd-first` 生成。`_(待补充)_` 处运行 `prd edit <字段>` 补全,或 `prd check` 查看完整度。_
